services:
  gerapop:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app/backend:ro
      - ./gerapop:/app/gerapop:ro
      - ./data:/app/data
      - ./POP - Procedimento Operacional Padrão:/app/POP - Procedimento Operacional Padrão
    restart: unless-stopped