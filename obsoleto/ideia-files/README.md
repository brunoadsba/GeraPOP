# Arquivo de referência — protótipo original (OBSOLETO)

> Arquivado em `obsoleto/ideia-files/` — **não usar**. Mantido apenas como
> referência histórica do protótipo; o código ativo vive na raiz do repositório.

O projeto foi reorganizado na raiz do repositório.

- Código ativo: `/app.py`, `/gerapop/`
- Documentação: `/README.md`, `/docs/plano.md`
- Execução: `make install-dev && make run`
